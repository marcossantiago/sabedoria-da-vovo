// ─── CONFIGURAÇÃO (template para novos forks) ──────────────────────────────────
// Copie para js/config.js e ajuste os valores.
// ───────────────────────────────────────────────────────────────────────────────

const SABEDORIA_CONFIG = {
  // URL do Cloudflare Worker — preenchido automaticamente no deploy.
  // Para desenvolvimento local: http://localhost:8787
  apiEndpoint: '',

  // Nome do site e da vovó
  siteName: 'Sabedoria da Vovó',
  grandmaName: 'Vovó',
  grandmaAge: 91,
};
